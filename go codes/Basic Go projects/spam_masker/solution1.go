package main

// func main() {
// 	args := os.Args[1:]
// 	if len(args) != 1 {
// 		fmt.Println("Give me something to mask!!!")
// 		return
// 	}
// 	const (
// 		link  = "http://"
// 		nlink = len(link)
// 	)
// fmt.Println(link, nlink)

// var (
// 	text = args[0]
// 	size = len(text)
// 	buf  = make([]byte, 0, size)
// )

// fmt.Printf("Text :%s Size(Len)  :%d\n", text, size)
// fmt.Println(buf)
// 	for i := 0; i < size; i++ {
// 		buf = append(buf, text[i])

// 		if len(text[i:]) >= nlink && text[i:i+nlink] == link {
// 		}
// 	}
// 	fmt.Println(string(buf))

// }
